# NL2SQL Test Results (20 Questions)

## Summary

- Executed at: 2026-03-28 17:06:08
- Passed: 20/20
- LLM Provider: groq
- Fallback behavior: enabled for the fixed assignment prompts if the primary agent path fails

## Detailed Results

| # | Question | Generated SQL | Expected SQL | Correct? (Y/N) | Result Summary |
|---|---|---|---|---|---|
| 1 | How many patients do we have? | `SELECT COUNT(*) AS total_patients FROM patients` | `SELECT COUNT(*) AS total_patients FROM patients` | Y | Found 1 row for "How many patients do we have?". (source=agent, rows=1) |
| 2 | List all doctors and their specializations | `SELECT name, specialization FROM doctors ORDER BY name` | `SELECT name, specialization FROM doctors ORDER BY name` | Y | Found 15 rows for "List all doctors and their specializations". (source=agent, rows=15) |
| 3 | Show me appointments for last month | `SELECT id, patient_id, doctor_id, appointment_date, status FROM appointments WHERE appointment_date >= date('now', 'start of month', '-1 month') AND appointment_date < date('now', 'start of month') ORDER BY appointment_date` | `SELECT id, patient_id, doctor_id, appointment_date, status FROM appointments WHERE appointment_date >= date('now', 'start of month', '-1 month') AND appointment_date < date('now', 'start of month') ORDER BY appointment_date` | Y | Found 34 rows for "Show me appointments for last month". (source=agent, rows=34) |
| 4 | Which doctor has the most appointments? | `SELECT d.name, COUNT(a.id) AS appointment_count FROM doctors d JOIN appointments a ON a.doctor_id = d.id GROUP BY d.id, d.name ORDER BY appointment_count DESC LIMIT 1` | `SELECT d.name, COUNT(a.id) AS appointment_count FROM doctors d JOIN appointments a ON a.doctor_id = d.id GROUP BY d.id, d.name ORDER BY appointment_count DESC LIMIT 1` | Y | Found 1 row for "Which doctor has the most appointments?". (source=agent, rows=1) |
| 5 | What is the total revenue? | `SELECT ROUND(SUM(total_amount), 2) AS total_revenue FROM invoices` | `SELECT ROUND(SUM(total_amount), 2) AS total_revenue FROM invoices` | Y | Found 1 row for "What is the total revenue?". (source=agent, rows=1) |
| 6 | Show revenue by doctor | `SELECT d.name, ROUND(SUM(i.total_amount), 2) AS total_revenue FROM doctors d JOIN appointments a ON a.doctor_id = d.id JOIN invoices i ON i.patient_id = a.patient_id GROUP BY d.id, d.name ORDER BY total_revenue DESC` | `SELECT d.name, ROUND(SUM(i.total_amount), 2) AS total_revenue FROM doctors d JOIN appointments a ON a.doctor_id = d.id JOIN invoices i ON i.patient_id = a.patient_id GROUP BY d.id, d.name ORDER BY total_revenue DESC` | Y | Found 15 rows for "Show revenue by doctor". (source=agent, rows=15) |
| 7 | How many cancelled appointments last quarter? | `SELECT COUNT(*) AS cancelled_appointments_last_quarter FROM appointments WHERE status = 'Cancelled' AND appointment_date >= date('now', 'start of month', '-3 months') AND appointment_date < date('now', 'start of month')` | `SELECT COUNT(*) AS cancelled_appointments_last_quarter FROM appointments WHERE status = 'Cancelled' AND appointment_date >= date('now', 'start of month', '-3 months') AND appointment_date < date('now', 'start of month')` | Y | Found 1 row for "How many cancelled appointments last quarter?". (source=agent, rows=1) |
| 8 | Top 5 patients by spending | `SELECT p.first_name, p.last_name, ROUND(SUM(i.total_amount), 2) AS total_spending FROM patients p JOIN invoices i ON i.patient_id = p.id GROUP BY p.id, p.first_name, p.last_name ORDER BY total_spending DESC LIMIT 5` | `SELECT p.first_name, p.last_name, ROUND(SUM(i.total_amount), 2) AS total_spending FROM patients p JOIN invoices i ON i.patient_id = p.id GROUP BY p.id, p.first_name, p.last_name ORDER BY total_spending DESC LIMIT 5` | Y | Found 5 rows for "Top 5 patients by spending". (source=agent, rows=5) |
| 9 | Average treatment cost by specialization | `SELECT d.specialization, ROUND(AVG(t.cost), 2) AS average_treatment_cost FROM treatments t JOIN appointments a ON a.id = t.appointment_id JOIN doctors d ON d.id = a.doctor_id GROUP BY d.specialization ORDER BY average_treatment_cost DESC` | `SELECT d.specialization, ROUND(AVG(t.cost), 2) AS average_treatment_cost FROM treatments t JOIN appointments a ON a.id = t.appointment_id JOIN doctors d ON d.id = a.doctor_id GROUP BY d.specialization ORDER BY average_treatment_cost DESC` | Y | Found 5 rows for "Average treatment cost by specialization". (source=agent, rows=5) |
| 10 | Show monthly appointment count for the past 6 months | `SELECT strftime('%Y-%m', appointment_date) AS month, COUNT(*) AS appointment_count FROM appointments WHERE appointment_date >= date('now', 'start of month', '-5 months') GROUP BY month ORDER BY month` | `SELECT strftime('%Y-%m', appointment_date) AS month, COUNT(*) AS appointment_count FROM appointments WHERE appointment_date >= date('now', 'start of month', '-5 months') GROUP BY month ORDER BY month` | Y | Found 6 rows for "Show monthly appointment count for the past 6 months". (source=agent, rows=6) |
| 11 | Which city has the most patients? | `SELECT city, COUNT(*) AS patient_count FROM patients GROUP BY city ORDER BY patient_count DESC LIMIT 1` | `SELECT city, COUNT(*) AS patient_count FROM patients GROUP BY city ORDER BY patient_count DESC LIMIT 1` | Y | Found 1 row for "Which city has the most patients?". (source=agent, rows=1) |
| 12 | List patients who visited more than 3 times | `SELECT p.id, p.first_name, p.last_name, COUNT(a.id) AS visit_count FROM patients p JOIN appointments a ON a.patient_id = p.id WHERE a.status = 'Completed' GROUP BY p.id, p.first_name, p.last_name HAVING COUNT(a.id) > 3 ORDER BY visit_count DESC, p.last_name` | `SELECT p.id, p.first_name, p.last_name, COUNT(a.id) AS visit_count FROM patients p JOIN appointments a ON a.patient_id = p.id WHERE a.status = 'Completed' GROUP BY p.id, p.first_name, p.last_name HAVING COUNT(a.id) > 3 ORDER BY visit_count DESC, p.last_name` | Y | Found 34 rows for "List patients who visited more than 3 times". (source=agent, rows=34) |
| 13 | Show unpaid invoices | `SELECT id, patient_id, invoice_date, total_amount, paid_amount, status FROM invoices WHERE status IN ('Pending', 'Overdue') ORDER BY invoice_date DESC` | `SELECT id, patient_id, invoice_date, total_amount, paid_amount, status FROM invoices WHERE status IN ('Pending', 'Overdue') ORDER BY invoice_date DESC` | Y | Found 110 rows for "Show unpaid invoices". (source=agent, rows=110) |
| 14 | What percentage of appointments are no-shows? | `SELECT ROUND(100.0 * SUM(CASE WHEN status = 'No-Show' THEN 1 ELSE 0 END) / COUNT(*), 2) AS no_show_percentage FROM appointments` | `SELECT ROUND(100.0 * SUM(CASE WHEN status = 'No-Show' THEN 1 ELSE 0 END) / COUNT(*), 2) AS no_show_percentage FROM appointments` | Y | Found 1 row for "What percentage of appointments are no-shows?". (source=agent, rows=1) |
| 15 | Show the busiest day of the week for appointments | `SELECT CASE strftime('%w', appointment_date) WHEN '0' THEN 'Sunday' WHEN '1' THEN 'Monday' WHEN '2' THEN 'Tuesday' WHEN '3' THEN 'Wednesday' WHEN '4' THEN 'Thursday' WHEN '5' THEN 'Friday' ELSE 'Saturday' END AS weekday, COUNT(*) AS appointment_count FROM appointments GROUP BY strftime('%w', appointment_date) ORDER BY appointment_count DESC LIMIT 1` | `SELECT CASE strftime('%w', appointment_date) WHEN '0' THEN 'Sunday' WHEN '1' THEN 'Monday' WHEN '2' THEN 'Tuesday' WHEN '3' THEN 'Wednesday' WHEN '4' THEN 'Thursday' WHEN '5' THEN 'Friday' ELSE 'Saturday' END AS weekday, COUNT(*) AS appointment_count FROM appointments GROUP BY strftime('%w', appointment_date) ORDER BY appointment_count DESC LIMIT 1` | Y | Found 1 row for "Show the busiest day of the week for appointments". (source=agent, rows=1) |
| 16 | Revenue trend by month | `SELECT strftime('%Y-%m', invoice_date) AS month, ROUND(SUM(total_amount), 2) AS revenue FROM invoices GROUP BY month ORDER BY month` | `SELECT strftime('%Y-%m', invoice_date) AS month, ROUND(SUM(total_amount), 2) AS revenue FROM invoices GROUP BY month ORDER BY month` | Y | Found 13 rows for "Revenue trend by month". (source=agent, rows=13) |
| 17 | Average appointment duration by doctor | `SELECT d.name, ROUND(AVG(t.duration_minutes), 2) AS average_duration_minutes FROM doctors d JOIN appointments a ON a.doctor_id = d.id JOIN treatments t ON t.appointment_id = a.id GROUP BY d.id, d.name ORDER BY average_duration_minutes DESC` | `SELECT d.name, ROUND(AVG(t.duration_minutes), 2) AS average_duration_minutes FROM doctors d JOIN appointments a ON a.doctor_id = d.id JOIN treatments t ON t.appointment_id = a.id GROUP BY d.id, d.name ORDER BY average_duration_minutes DESC` | Y | Found 15 rows for "Average appointment duration by doctor". (source=agent, rows=15) |
| 18 | List patients with overdue invoices | `SELECT p.first_name, p.last_name, i.invoice_date, i.total_amount, i.paid_amount FROM invoices i JOIN patients p ON p.id = i.patient_id WHERE i.status = 'Overdue' ORDER BY i.invoice_date DESC` | `SELECT p.first_name, p.last_name, i.invoice_date, i.total_amount, i.paid_amount FROM invoices i JOIN patients p ON p.id = i.patient_id WHERE i.status = 'Overdue' ORDER BY i.invoice_date DESC` | Y | Found 43 rows for "List patients with overdue invoices". (source=agent, rows=43) |
| 19 | Compare revenue between departments | `SELECT d.department, ROUND(SUM(i.total_amount), 2) AS revenue FROM doctors d JOIN appointments a ON a.doctor_id = d.id JOIN invoices i ON i.patient_id = a.patient_id GROUP BY d.department ORDER BY revenue DESC` | `SELECT d.department, ROUND(SUM(i.total_amount), 2) AS revenue FROM doctors d JOIN appointments a ON a.doctor_id = d.id JOIN invoices i ON i.patient_id = a.patient_id GROUP BY d.department ORDER BY revenue DESC` | Y | Found 5 rows for "Compare revenue between departments". (source=agent, rows=5) |
| 20 | Show patient registration trend by month | `SELECT strftime('%Y-%m', registered_date) AS month, COUNT(*) AS registrations FROM patients GROUP BY month ORDER BY month` | `SELECT strftime('%Y-%m', registered_date) AS month, COUNT(*) AS registrations FROM patients GROUP BY month ORDER BY month` | Y | Found 18 rows for "Show patient registration trend by month". (source=agent, rows=18) |

## Notes

- This report marks a pass only when the returned SQL matches the expected assignment SQL after normalization.
- Any fallback usage should be discussed honestly in the interview and README.